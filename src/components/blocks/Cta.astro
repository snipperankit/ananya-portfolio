---
const { block } = Astro.props;
const alignClass = block.align === 'center' ? 'text-center' : block.align === 'right' ? 'text-right' : '';
---
<div class={`my-8 ${alignClass}`}>
  <a href={block.href} class="btn-accent">{block.label}</a>
</div>
